#!/bin/tcsh

if ($1 == '' || $2 == '' || $3 == '' | $4 == '') then
	echo "Usage: ./run.sh [gphh.sh | gphhStat.sh] [vmtypes = TEN | TWENTY] [OSNUM = 5] [dataset = BITBRAINS | AUVERGRID]"
	exit
endif
@ run=40
@ i=30


while ($i < $run)
	qsub $1 $2 $3 $4 $i
	@ i++
end
